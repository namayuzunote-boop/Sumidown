# Sumidown 開発憲法

このファイルはこのリポジトリで作業するすべてのエージェント/開発者への最上位の指針です。
個別の判断に迷ったら、ここに書かれた原則を優先してください。

## 1. このプロダクトは何か

無料・MIT・**軽量** な WYSIWYG Markdown エディタ。Tauri 2(Rust + OS WebView)製。
コーディングエージェント時代の Markdown ワークフローにある3課題を解決するために存在する:

1. PDF 変換でレイアウトが崩れる → プレビューと同一レンダラで印刷し構造的に崩さない
2. プレビュー状態で編集できない → 常時 WYSIWYG(テーブル・図・数式も編集可)
3. 図が表示できない → Mermaid を即時レンダリング

この3点が「なぜこのアプリが要るのか」の核心。新機能はこの価値を薄めないこと。

## 2. 4つの価値(設計判断の優先順位)

- **モダン** — 古びた UI/手法を避ける。最新の安定版ライブラリ・API を使う。AI 連携を意識する。
- **軽量** — バイナリは小さく(現状 ~12MB)。重い依存(Electron、巨大ランタイム、不要な UI フレームワーク)を足さない。新しい依存を入れる前に「本当に必要か」「自前の数十行で済まないか」を必ず問う。
- **シンプル** — 機能も実装も最小限で明快に。設定地獄・多機能化を避ける。1ファイル1責務。読んで分かるコード。
- **高品質(TDD)** — 下記 §3 を徹底。バグは「直す」だけでなく「二度と起きないテスト」を残す。

迷ったら、機能の豊富さより **軽量・シンプル** を取る。

## 3. TDD を徹底する

このプロジェクトはテスト駆動で品質を担保する。**回帰を許さない**のが最優先。

- **バグ修正は必ずテストから**。まず失敗する(red)テストで不具合を再現し、それから直す(green)。
  「タスクリストのチェックボックスが出ない」のような表示崩れも、`src/editor/taskList.test.ts` のように
  レンダリング結果の DOM 属性 / CSS 契約を検証するテストで固定する。
- **新機能はテスト可能な単位に切る**。純ロジック(例: `sourceMap.ts`)は DOM 非依存の純関数に保ち、単体テストする。
- **コミット前に必ず緑**にする。`npm test`(フロント)と `cargo test`(Rust)の両方。CI でも実行される(`.github/workflows/build.yml`)。
- テストは仕様書でもある。何を保証しているかコメントで明記する。

### テスト構成
- フロント: **Vitest + jsdom**。`src/**/*.test.ts(x)`。`npm test` / `npm run test:watch`。
  - 純ロジックは直接呼んで検証(`sourceMap.test.ts`)。
  - レンダリングは最小構成の Milkdown を jsdom で起動して DOM を検証(`taskList.test.ts`)。
  - CSS で描く UI(タスクのチェックボックス等)は、スタイルシートを文字列で読み「ルールが存在すること」も契約テストにする。
- Rust: 標準の `#[cfg(test)]`。パス・スコープ検証など(`src-tauri/src/lib.rs`)。

## 4. アーキテクチャ上の不変条件(壊すな)

これらは過去にバグを生んだ箇所。変更時は対応するテスト/手動確認を必ず行う。

- **プレビュー = エクスポート = 印刷は同一の見た目**。本文スタイルは `src/styles/editor.css` 1枚を3者で共有する。
  本文の見た目を変える時は PDF 出力(⌘E)も必ず確認。
- **`@media print` は無条件記述**(`src/styles/print.css`)。クラスやタイマーで印刷モードを出し入れしない
  (macOS の印刷ダイアログは `window.print()` 復帰後も開いたままで、保存時に再レンダリングされるため)。
- **カーソル位置の相互変換は単一の content 文字列基準**(`src/editor/sourceMap.ts`、remark の position)。
  doc の部分シリアライズは remark がクラッシュするので使わない。
- **Milkdown のスキーマ専用プラグイン**(diagram / math)は**レンダラを含まない**。ノードビューを `*View.ts` で自作する。
  同様に headless コンポーネント(table-block 等)のスタイルは `editor.css` に自前で書く。
- **ファイルアクセスは「開いたフォルダ配下」にスコープ制限**(Rust `scope_check`)。緩める変更は禁止。テスト必須。

## 5. 品質ゲート(マージ前チェック)

1. `npm test` が緑(フロント単体テスト)
2. `cargo test --manifest-path src-tauri/Cargo.toml` が緑(Rust 単体テスト)
3. `npm run build` が通る(`tsc` 型チェック + バンドル。テストファイルも型チェック対象)
4. 表示に関わる変更は、ライト/ダーク両方とブラウザプレビュー(`npm run dev`)で確認
5. 本文・テーブル・図に関わる変更は **PDF 出力**も確認

## 6. コミュニケーション

- ユーザーへの応答・説明はすべて**日本語**。
- コード内コメントは英語(既存コードに合わせる)。ユーザー向け UI 文字列は日本語。

## 7. 補足

- スタック: Tauri 2 / React 19 + TypeScript / Vite / @milkdown/kit(ProseMirror)/ CodeMirror 6 / Mermaid / KaTeX。Crepe は不使用(Mermaid 非対応のため)。
- 対応 OS は現状 macOS のみ(パス処理が Unix 前提)。Windows/Linux 対応時はパス処理の抽象化が前提。
- ライセンス監査: 配布物の第三者ライセンスは `THIRD-PARTY-LICENSES.txt`。依存追加時は更新を検討。
